# Single-package repo (no workspace packages). This file exists only to host
# dependency overrides, whose home moved here in pnpm 10.12+.
overrides:
  postcss@<8.5.10: ">=8.5.10"
  "@hono/node-server@<1.19.13": ">=1.19.13"
  # Dedupe ioredis so bullmq and the app share one copy (avoids type clash).
  ioredis: 5.11.1
